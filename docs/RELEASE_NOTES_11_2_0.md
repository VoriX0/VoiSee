# VoiSee 11.2.0

Adds Media Bridge profiles as an optional scene background source. Existing looped-sound scenes retain their previous behavior.

No build, smoke tests, or automated validation were run at the user’s request.
