# VoiSee 12.3.2 — Direct panel scrolling

This release restructures scrolling so each requested content panel is immediately enclosed by the ScrollViewer that owns it. It removes the SoundBoard input overlay, removes the disabled Voice Changer outer scroller and obsolete wheel-zone marker elements, and adds a direct scene-list ScrollViewer.
